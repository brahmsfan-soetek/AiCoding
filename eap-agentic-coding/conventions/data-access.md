# eap 資料存取與回應慣例

## ORM 策略

<!-- TODO: 從 EAP 專案填入 — 預設 ORM 模式、何時用原生 SQL -->

## Entity 設計

<!-- TODO: 從 EAP 專案填入，以下為需確認的項目：

- Entity 模式（Active Record / Repository Pattern / 其他）
- 靜態方法 vs 實例方法策略
- Entity → DTO/Map 轉換策略（如 toMap / toDTO）
- 複雜邏輯放置位置（Service 層 / Entity 內 / 其他）
-->

## 回應格式

<!-- TODO: 從 EAP 專案填入，以下為需確認的項目：

- 分頁查詢結果的 key（如 records / data / items）
- 下拉選單/LOV 的 key（如 items / options）
- 日期格式化工具與規則
- 分頁策略（前端分頁 / 後端分頁）
-->

## 共用組件版本

<!-- TODO: 從 EAP 專案填入 — 列出前端共用組件及其版本規則（如表格、Dialog、輸入框、按鈕等），格式：

| 需求 | 使用 | 不使用 |
|------|------|--------|
| 表格 | **XxxTable** | — |
| Dialog | **XxxDialog** | — |
| ... | ... | ... |
-->
