# {程式編號} {功能名稱} — 前端實作清單

> 初版日期：{YYYY-MM-DD}

---

<!-- 說明：區塊 A — TypeScript 類型定義與 API 服務層。每個程式通常至少有一個 types 檔與一個 service 檔。 -->
## A. 類型定義與服務

| # | 項目 | 說明 |
|---|------|------|
| F01 | `{程式編號小寫}Types.ts` | TypeScript 介面定義：{列出主要介面名稱，如 Header、Detail、QueryParams、FormData 及各 LOV 回傳型別} |
| F02 | `{程式編號小寫}Service.ts` | API 服務層：{列出主要方法，如 query、detail、create、update、delete、validate、{業務動作} 及各 LOV 呼叫} |

---

<!-- 說明：區塊 B — 查詢頁面元件拆分。依專案元件命名規範填入，需涵蓋頁面入口、主查詢、篩選區、按鈕、查詢結果表格。 -->
## B. 查詢頁面

| # | 項目 | 說明 |
|---|------|------|
| F03 | `{程式編號}.vue` | 頁面入口 wrapper |
| F04 | `{程式編號}searchComponent.vue` | 主查詢頁面：篩選區 + 結果 TablesComponent |
| F05 | `{程式編號}searchelementComponent.vue` | 篩選條件：{列出各篩選欄位名稱與元件類型，如文件編號(SInput2)、狀態(Selection)、日期起迄(Date)} |
| F06 | `{程式編號}searchbtnComponent.vue` | 查詢 / 新增按鈕 |
| F07 | 查詢結果表格 | 欄位：{列出表格欄位}；依 {狀態欄位} 條件顯示操作按鈕：{狀態值A}→{動作}、{狀態值B}→{動作} |

---

<!-- 說明：區塊 C — 新增/編輯/檢視頁面。需列出主頁面元件、表頭區塊、表身明細區塊、摘要區塊、按鈕列，並附上兩張條件欄位對照表（表頭條件 + 表身條件顯示）。 -->
## C. 新增 / 編輯 / 檢視頁面

| # | 項目 | 說明 |
|---|------|------|
| F08 | `{程式編號}dialogComponent.vue` | 新增/編輯主頁面（或獨立路由頁面，依 UI 設計） |
| F09 | 表頭區塊 | {條件分類欄位}(Selection) + 共用欄位（{列出共用欄位}）+ 依{條件分類}條件顯示額外欄位 |
| F10 | 表身明細區塊 | TablesComponent 動態行：{列出表身欄位，含 LOV 欄位、計算欄位、唯讀欄位、刪除按鈕} |
| F11 | 摘要區塊 | 頁面底部：{列出摘要統計欄位，如總筆數、總數量、總金額} |
| F12 | 按鈕列 | {列出按鈕，如儲存、{業務動作}、驗證、取消、新增明細行} |

### 表頭欄位 — {條件分類} vs 條件欄位對照

<!-- 說明：此表列出不同條件分類（如業務類型、單據類型）下，表頭需顯示的額外欄位。共用欄位欄填入所有分類皆顯示的欄位，額外欄位欄填入該分類專屬欄位及其元件類型與對應資料庫欄位。 -->

| {條件分類} | 共用欄位 | 額外欄位 |
|-----------|---------|---------|
| {類型值A} | {列出共用欄位} | {額外欄位名}({元件類型} → {對應DB欄位}) |
| {類型值B} | {列出共用欄位} | {額外欄位名1}({元件類型} → {DB欄位})、{額外欄位名2}({元件類型} → {DB欄位}) |

### 表身欄位 — 條件顯示

<!-- 說明：此表列出各表身欄位在不同條件下的顯示規則。「全類型」欄填 ✓ 表示所有條件分類皆顯示；「僅{條件值}」欄填 ✓ 表示僅特定條件下顯示。說明欄填入欄位行為，如：LOV 帶入/readonly、計算規則、必填條件等。 -->

| 欄位 | 全類型 | 僅{條件值} | 說明 |
|------|:-----:|:---------:|------|
| {欄位名A} | ✓ | | {欄位行為說明，如 LOV 帶入，readonly} |
| {欄位名B} | ✓ | | {欄位行為說明，如 正數，必填} |
| {欄位名C（條件欄位）} | | ✓ | {欄位行為說明} |

---

<!-- 說明：區塊 D — 前端連動邏輯。每條連動用四欄描述：# 為編號，項目為觸發來源欄位，觸發動作為用戶操作，連動結果為系統回應（清空、帶入、切換顯示等）。 -->
## D. 前端連動邏輯

| # | 項目 | 觸發動作 | 連動結果 |
|---|------|---------|---------|
| F13 | {欄位/元件名稱} | {用戶操作，如 變更選項、選擇 LOV、輸入數值} | {系統回應，如 清除畫面資料、帶入欄位值、更新下游 LOV 篩選條件} |
| F14 | {欄位/元件名稱} | {用戶操作} | {系統回應} |

---

<!-- 說明：區塊 E — 前端驗證規則。涵蓋必填檢查、格式檢查、跨欄位一致性、狀態聯鎖（如已過帳唯讀）等。每條說明欄需足夠具體，說明觸發時機與錯誤提示文字。 -->
## E. 前端驗證

| # | 項目 | 說明 |
|---|------|------|
| F15 | 必填檢查 | 表頭：{必填欄位清單}；表身：至少一筆明細行、各行必填欄位（{欄位清單}） |
| F16 | {驗證規則名稱} | {觸發時機}：{驗證邏輯說明}（{不送後端 / 送後端驗證}） |

---

<!-- 說明：區塊 F — 國際化語系檔與路由設定。依專案目錄結構填入實際路徑。 -->
## F. i18n / 路由

| # | 項目 | 說明 |
|---|------|------|
| F17 | i18n 語系檔 | `zh-TW/{模組}/{程式編號小寫}.ts`、`en-US/{模組}/{程式編號小寫}.ts` |
| F18 | 路由設定 | `routes.ts` 新增 {程式編號} 路由 |
