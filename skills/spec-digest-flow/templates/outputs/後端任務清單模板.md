# {程式編號} {功能名稱} — 後端實作清單

> 初版日期：{YYYY-MM-DD}

---

<!-- 說明：區塊 A — Entity / Domain 層。每個資料庫寫入對象至少對應一個 Entity。填入類別名稱、對應資料表、主要欄位列表，以及繼承的基類（如 AuditableEntity）。若有交易記錄表或日誌表亦需列出。 -->
## A. Entity / Domain 層

| # | 項目 | 說明 |
|---|------|------|
| B01 | `{功能}HeaderEntity` | {功能} 表頭 Entity，對應 `{資料表名}`，含 {主要欄位清單}，繼承 `AuditableEntity` |
| B02 | `{功能}DetailEntity` | {功能} 明細 Entity，對應 `{資料表名}`，PK 為 {主鍵說明}，含 {主要欄位清單} |

---

<!-- 說明：區塊 B — Processor 層（API 端點）。依業務操作逐一列出：查詢（列表）、取得（單筆）、新增、修改、刪除、驗證、核心業務動作（如過帳、審核）、下拉選單。每個 Processor 說明其查詢邏輯、鎖定方式、前提條件等重點。 -->
## B. Processor 層（API 端點）

| # | 項目 | 說明 |
|---|------|------|
| B03 | `{程式編號}QueryProcessor` | 查詢頁面：依篩選條件（{篩選欄位清單}）查詢 {表頭表} + {彙總欄位}，{分頁/不分頁} |
| B04 | `{程式編號}DetailProcessor` | 取得單筆完整資料：{表頭表} + {明細表} list，供編輯/檢視頁面使用 |
| B05 | `{程式編號}CreateProcessor` | 新增：{取號方式} 取 {主鍵欄位} → 寫入 {表頭表} + {明細表}，{初始狀態}，{鎖定方式} |
| B06 | `{程式編號}UpdateProcessor` | 修改：{鎖定方式} → 更新 {表頭表} + {明細表}（{更新策略：先刪後寫/差異更新}），限 {前提條件} |
| B07 | `{程式編號}DeleteProcessor` | 刪除：{刪除方式：硬刪除/軟刪除} {明細表} + {表頭表}，限 {前提條件} |
| B08 | `{程式編號}ValidateProcessor` | 驗證：做{業務動作}前業務面檢查（{檢查項目清單}），不實際寫入 |
| B09 | `{程式編號}{業務動作}Processor` | **{業務動作}（核心）**：單一 LUW，{步驟摘要，如 前置檢查→建交易記錄→更新成本→建傳票→更新庫存→COMMIT/ROLLBACK} |
| B10 | `{程式編號}DropdownProcessor` | {功能} 下拉：查 `{參數表}` WHERE {篩選條件} |

---

<!-- 說明：區塊 C — LOV Processor 層。每個 LOV 獨立列出，說明來源表、篩選條件、回傳欄位及帶入目標欄位。若有依上游欄位動態篩選的 LOV，需說明依賴關係。 -->
## C. LOV Processor 層

| # | 項目 | 說明 |
|---|------|------|
| B11 | `{程式編號}{實體名}LovProcessor` | LOV {編號} {欄位名}：{來源表 JOIN 說明}，篩選條件 {條件}，回傳 {回傳欄位}，帶入 {目標欄位} |
| B12 | `{程式編號}{實體名}LovProcessor` | LOV {編號} {欄位名}：依 {上游欄位} 篩選 {來源表}，{特殊篩選條件說明} |

---

<!-- 說明：區塊 D — 核心業務邏輯（如過帳、審核等主要 Processor 的內部拆分）。用子編號（{Bxx}-1、{Bxx}-2…）逐步拆分，從前置檢查到最終提交，每個子步驟說明操作對象、邏輯規則與寫入目標。此區塊直接對應後續測試清單的業務流程測試案例。 -->
## D. 核心業務邏輯（B09 {業務動作} 內部拆分）

| # | 項目 | 說明 |
|---|------|------|
| B09-1 | 前置檢查 | {說明需檢查的前置條件，如 查 {期間表} 確認期間開放；後端庫存充足檢查（依{業務類型}查 {庫存表}.{數量欄位}）} |
| B09-2 | 狀態更新 | {表頭表}.{狀態欄位} → '{完成狀態值}' |
| B09-3 | 交易記錄建立 | {說明每筆明細產生幾筆交易、各筆的欄位值規則、交易 ID 取號方式} |
| B09-4 | 成本/數據更新 | {說明呼叫的成本計算服務或直接更新的目標表與欄位；D-type 不呼叫時標明} |
| B09-5 | 科目/對照查找 | {說明科目取得邏輯：取{分類欄位} → 查{對照表}取{指標欄位}/{鍵值} → 查{科目表}取存貨科目；費用科目由{鍵值條件}決定} |
| B09-6 | 傳票/文件建立 | {說明傳票 Header 欄位來源（如 JE_NO 取號、DOC_TYPE、SOURCE_TYPE）及 Detail 兩筆分錄的借貸規則} |
| B09-7 | 外部程序呼叫（如 SP） | {說明需呼叫的 DB 預存程序或外部服務，涵蓋哪些資料表；失敗時納入 LUW 整體 ROLLBACK} |

---

<!-- 說明：區塊 E — SP 替換評估（可選）。評估現有 Java 實作是否可改呼叫 DB 預存程序，降低維護複雜度。若本次不評估，整個區塊可省略。「可替換項目」表說明替換對象與預期效果；「不適用項目」表說明排除原因。 -->
## E. SP 替換評估（可選）

> 參考：`Docs/stored-procedures/00_SpIndex.md`（若有）

以下功能目前以 {語言/框架} 實作，可評估改為呼叫 DB 預存程序。替換後可減少應用層程式碼量並統一邏輯歸口。

### 可替換項目

| # | SP 名稱 | 替換對象 | 影響任務 | 替換效果 | 狀態 |
|---|--------|---------|---------|---------|------|
| SP-1 | `{SP名稱}` | `{替換的類別/方法名稱}` | {影響的任務編號，如 B05、B09-3} | {替換後可刪除/簡化的程式碼說明} | 待確認 SP 參數規格 |

### 不適用項目

| SP 名稱 | 不適用原因 |
|--------|-----------|
| `{SP名稱}` | {原因，如 屬於其他模組專用，與本程式無關} |

### 替換後應用層異動估算

若 SP-1 {及其他} 全部替換：

| 異動 | 說明 |
|------|------|
| 可刪除 | `{類別/檔案名稱}`（{說明}） |
| 可大幅簡化 | `{類別/檔案名稱}`（{步驟} 約 {N} 行 → 改為 ~{M} 行 {SP} 呼叫） |
| SQL YAML 可移除 | `{SQL 段落名稱1}`、`{SQL 段落名稱2}`（共 {N} 段） |

> ⚠ 以上替換需等 SA 提供各 SP 的完整輸入/輸出參數規格後方可執行。
