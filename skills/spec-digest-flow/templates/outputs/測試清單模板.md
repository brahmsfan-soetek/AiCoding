# {程式編號} {功能名稱} — 業務邏輯測試清單

> 初版日期：{YYYY-MM-DD}

---

<!-- 說明：區塊 A — 基礎 CRUD 測試。涵蓋查詢（無條件、各篩選條件、多條件組合）、依各業務類型的新增、修改（成功與拒絕修改已鎖定單據）、刪除（成功與拒絕刪除已鎖定單據）。 -->
## A. 基礎 CRUD

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T01 | 查詢 — 無篩選條件 | 回傳所有記錄，含彙總欄位（如 COUNT、SUM） |
| T02 | 查詢 — 依{篩選欄位}篩選 | 僅回傳符合條件的記錄 |
| T03 | 查詢 — 多條件組合篩選 | 條件交集正確 |
| T04 | 新增 — {業務類型A} 正常儲存 | 產生{主鍵欄位}、{初始狀態}，{表頭表}含對應欄位，{明細表}含對應欄位 |
| T05 | 修改 — 修改{可修改狀態}單據 | 正確更新{表頭表} + {明細表}，狀態不變 |
| T06 | 修改 — 嘗試修改{鎖定狀態}單據 | 拒絕，回傳錯誤訊息 |
| T07 | 刪除 — 刪除{可刪除狀態}單據 | {表頭表} + {明細表}全部刪除，DB 無殘留 |
| T08 | 刪除 — 嘗試刪除{鎖定狀態}單據 | 拒絕，回傳錯誤訊息 |

---

<!-- 說明：區塊 B — 編碼取號測試。針對所有以 AU_GEN_NUMBER 或同類機制產生的流水號，驗證格式、連續性、上限保護、以及併發時的唯一性。 -->
## B. 編碼取號

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T09 | {主鍵欄位} 取號格式 | 格式符合規格定義（如 前綴 + YYYYMM + N 碼遞增） |
| T10 | {主鍵欄位} 連續取號 | 第二筆序號正確遞增，無跳號 |
| T11 | 取號到達上限 (END_NUMBER) | 回傳錯誤，不允許超過上限 |
| T12 | 併發取號 | SELECT FOR UPDATE 確保序號不重複 |

---

<!-- 說明：區塊 C — 驗證規則測試。逐一驗證每個必填欄位、格式限制、跨欄位一致性規則。必填缺漏需標示行號與欄位；數值規則需含邊界值（如 0、負數）。 -->
## C. 驗證規則

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T13 | 儲存 — 缺少{必填表頭欄位} | 驗證失敗，提示「{欄位名}必填」 |
| T14 | 儲存 — 無明細行 | 驗證失敗，提示「至少一筆明細行」 |
| T15 | 明細行 — 缺少{必填明細欄位} | 驗證失敗，標示行號與欄位 |
| T16 | 明細行 — {數值欄位} 為 0 或負數 | 驗證失敗，提示「{欄位名}須為正數」 |
| T17 | {條件必填規則} — 條件成立時未填 | 驗證失敗，提示條件必填說明 |
| T18 | {條件必填規則} — 條件不成立時未填 | 驗證通過 |

---

<!-- 說明：區塊 D — 業務流程步驟一（前置檢查）。依業務動作（如過帳）的前置條件逐一驗證，包含：期間狀態、資源充足性（如庫存）、各業務類型的檢查差異，以及成功後的狀態欄位更新。 -->
## D. 業務流程 — 步驟一（前置檢查）

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T19 | {前置條件} 未通過 | {業務動作}失敗，提示「{錯誤訊息}」 |
| T20 | {前置條件} 通過 | 通過檢查，進入後續步驟 |
| T21 | {業務動作}後狀態欄位更新 | {表頭表}.{狀態欄位} 從 '{原狀態}' 變為 '{新狀態}' |

---

<!-- 說明：區塊 E — 業務流程步驟二（交易記錄建立）。驗證每種業務類型產生的交易記錄筆數與欄位值正確性，包含關鍵欄位的來源對照（如 TXN_DATE 來自文件日期、金額計算規則）。 -->
## E. 業務流程 — 步驟二（交易記錄建立）

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T22 | {業務類型A} — 每筆明細產生 {N} 筆交易 | {說明各筆交易的關鍵欄位值} |
| T23 | {業務類型B} — 每筆明細產生 {N} 筆交易 | {說明各筆交易的關鍵欄位值} |
| T24 | 交易記錄欄位正確性 | {列出需驗證的關鍵欄位，如 交易ID格式、日期來源、金額計算} |

---

<!-- 說明：區塊 F — 業務流程步驟三（成本/數量更新）。驗證各業務類型對成本或庫存數量表的影響，包含增減方向、計算公式、不影響的類型，以及稽核日誌是否正確寫入。 -->
## F. 業務流程 — 步驟三（成本 / 數量更新）

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T25 | {業務類型A} — {數量欄位} 正確更新 | {數量欄位} = 原值 {+-} {明細數量} |
| T26 | {業務類型B} — 不更新{成本/數量表} | {目標表} 完全不變 |
| T27 | 更新後寫入稽核日誌 | 日誌正確寫入，含異動前後值 |

---

<!-- 說明：區塊 G — 業務流程步驟四（文件/傳票建立）。驗證各業務類型的文件 Header 欄位（來源、類型識別碼）、Detail 分錄筆數與借貸規則、金額計算，以及不需建立文件的類型。 -->
## G. 業務流程 — 步驟四（文件 / 傳票建立）

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T28 | {業務類型A} — 不產生文件 | {文件 Header/Detail 表} 無新增記錄 |
| T29 | {業務類型B} — 文件 Header 正確 | {列出關鍵欄位：如 DOC_TYPE、SOURCE_TYPE、取號格式} |
| T30 | 文件 Detail — 兩筆分錄 | {科目類型A} + {科目類型B}，共兩筆 |
| T31 | 文件 Detail — 借貸方向相反 | {科目類型A} DC_INDICATOR 與 {科目類型B} DC_INDICATOR 相反 |
| T32 | 文件 Detail — 金額正確 | {金額欄位} = {計算公式} |

---

<!-- 說明：區塊 H — 交易一致性與併發。驗證整個業務動作（如過帳）為單一 LUW：任一步驟失敗時所有寫入需全部回滾。也需驗證悲觀鎖對併發操作的保護效果，以及多筆明細行的完整性。 -->
## H. 交易一致性 / 併發

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T33 | 步驟二失敗 → 全部回滾 | 狀態欄位仍為 '{原狀態}'，無交易記錄 |
| T34 | 步驟三失敗 → 全部回滾 | 狀態欄位仍為 '{原狀態}'，無交易記錄，{成本/數量表} 不變 |
| T35 | 步驟四失敗 → 全部回滾 | 狀態欄位仍為 '{原狀態}'，無交易/文件記錄，{成本/數量表} 不變 |
| T36 | 併發修改同一筆單據 | 後操作者被行鎖阻塞至前者完成，不產生資料衝突 |
| T37 | 多筆明細行完整性 | 所有明細正確產生對應的交易記錄、成本更新、文件分錄 |

---

<!-- 說明：區塊 I — LOV 與連動邏輯。逐一驗證各 LOV 的篩選條件、帶入欄位、依上游欄位動態篩選的行為（切換後下游重置），以及前端即時警告邏輯（無需送後端）。 -->
## I. LOV / 連動邏輯

| # | 測試案例 | 預期結果 |
|---|---------|---------|
| T38 | {LOV名稱} — {篩選條件} 排除 | 不符合條件的選項不出現在可選清單中 |
| T39 | {LOV名稱} — 帶入欄位正確 | 帶入 {欄位清單} 到對應的表單欄位 |
| T40 | {上游欄位} 切換 → {下游 LOV} 重置 | 切換後下游 LOV 選項重新載入，已選值清空 |
| T41 | {即時警告規則} | 條件成立時前端即時顯示警告（不送後端） |
| T42 | {條件排除規則} | 已選的{組合}在下游選項中被移除 |
