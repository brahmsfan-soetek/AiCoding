1-1 / 1-2 inv_no -> INVYYYYMM***, txn_id -> TXNYYYYMM***, 起始 001 遞增+1

1-3 正確的TYPE -> D01 庫存調撥單, D02 成本領用, D03 存貨報廢, D04 雜收, D05 雜發

1-4 表頭基本欄位: 異動類型、過帳日期、文件日期
D01-> + 調撥單號, D02-> + 成本中心 + 領用申請, D03-> +成本中心+報廢原因, D04->+利潤中心+收貨原因, D05->+利潤中心+發貨原因

1-5 If 異動類別 = 'D02' OR 異動類別 = 'D05' then @TXN_KEY = 'OEI', @ SUB_TXN_KEY = 'IGI'   -- 成本中心領用、雜發
ELSE IF  異動類別 = 'D03' then @TXN_KEY = 'OEI', @ SUB_TXN_KEY = 'DST'  -- 報廢
ELSE IF  異動類別 = 'D04' then @TXN_KEY = 'OEI', @ SUB_TXN_KEY = 'NPR'  -- 雜收

1-6 AGN 是指 Table AU_GEN_NUMBER 取號設定檔, 所以該專案的取號邏輯都來自於此；CREATE TABLE `AU_GEN_NUMBER` ( 
  `RANGE_ID` BIGINT AUTO_INCREMENT NOT NULL COMMENT '取號範圍ID' ,
  `FORM_TYPE` VARCHAR(3) NOT NULL COMMENT '表單名稱' ,
  `PREFIX` VARCHAR(15) NOT NULL COMMENT '前綴字' ,
  `N_YEAR` YEAR NOT NULL COMMENT '會計年度' ,
  `START_NUMBER` BIGINT NOT NULL COMMENT '起始編號' ,
  `END_NUMBER` BIGINT NOT NULL COMMENT '編號上限' ,
  `CURRENT_NUMBER` BIGINT NULL COMMENT '目前編號' ,
  `CREATOR` VARCHAR(20) NOT NULL COMMENT '建立人員' ,
  `CREATE_DT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP  COMMENT '建立日期' ,
  `UPDATER` VARCHAR(20) NOT NULL COMMENT '更新人員' ,
  `UPDATE_DT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP  COMMENT '更新日期' ,
  `COMPANY_CODE` VARCHAR(4) NOT NULL,
   PRIMARY KEY (`RANGE_ID`),
  CONSTRAINT `UK1_AU_GEN_NUMBER` UNIQUE (`FORM_TYPE`, `PREFIX`, `N_YEAR`)
)
ENGINE = InnoDB
COMMENT = '取號設定檔';

1-7 回去讀元規格處, 空白表示可修改, N表示不可修改
2-1 要加
2-2 標註 先跳過
2-3 要upsert
2-4 同一批次號
2-5 悲觀鎖, 可以進入但儲存時進行行鎖
2-6 來源選了, 目標那欄就從選項中移除
2-7 照SA設計的硬刪除, 表格都沒有軟刪除欄位
2-8 因為都要選來源所以應該是檢查庫位的庫存，確認原始規格書有沒有提到需要更新庫存，如果沒有就不屬於本程式範疇，我沒有找到有寫"usp_UpdateMovingAverageCost"

2-9 不太懂，先備註，應該是整筆
2-10 是，但例如 "AGN 序號是否能取得" 這種程式面的安全檢查應該交由過帳程式碼時檢查, 避免多次開關建立DB連線
2-11 不管, 全空就是查詢全部, 查詢沒寫說要必填就不管
2-12 這裡他讀錯了, 正確是 項目總數+總數量+總金額
3-1 DETAIL是對的
3-2 經過表格前綴修改後, IM/INV/MM 都是指 IM
3-3 先保持 REDUSE
3-4 先別管, 有共用元件區
3-5 UpdateMovingAverageCost 這東西到底哪來的
3-6 無
4-1 該程式中成本計算已被拿掉，如果仍有使用即向我確認
4-2 使用者不可修改單價
4-3 MP是誤植, 另表格有調整:
CREATE TABLE `GL_JOURNAL_HEADER` ( 
  `DOC_HEADER_ID` BIGINT AUTO_INCREMENT NOT NULL COMMENT '文件表頭ID' ,
  `JE_NO` VARCHAR(21) NOT NULL COMMENT '傳票號碼' ,
  `DOC_TYPE` VARCHAR(3) NOT NULL COMMENT '傳票類型' ,
  `COMPANY_CODE` VARCHAR(4) NOT NULL COMMENT '公司代碼' ,
  `PERIOD_YEAR` CHAR(4) NOT NULL COMMENT '會計年度' ,
  `LEDGER_GROUP_CODE` VARCHAR(20) NOT NULL COMMENT '帳本群組ID' ,
  `DESCRIPTION` VARCHAR(200) NULL COMMENT '說明' ,
  `JOURNAL_DATE` DATETIME NOT NULL COMMENT '分錄日期' ,
  `POSTING_DATE` DATETIME NOT NULL COMMENT '過帳日期' ,
  `PERIOD_SET_ID` INT NOT NULL COMMENT '會計年度ID' ,
  `PERIOD_NUM` CHAR(2) NOT NULL COMMENT '會計期別' ,
  `BASE_CURRENCY` VARCHAR(5) NOT NULL COMMENT '本位幣' ,
  `TXN_CURRENCY` VARCHAR(5) NOT NULL COMMENT '交易幣別' ,
  `DAILY_RATE` DECIMAL(18,6) NULL COMMENT '交易匯率' ,
  `RATE_DATE` DATE NULL COMMENT '匯率日期' ,
  `IS_REVERSED` CHAR(1) NOT NULL COMMENT '是否被迴轉' ,
  `REVERSED_HEADER_ID` VARCHAR(21) NULL COMMENT '執行迴轉之傳票ID' ,
  `REVERSED_PERIOD_NAME` VARCHAR(30) NULL COMMENT '執行迴轉之會計期間' ,
  `SOURCE_TYPE` VARCHAR(10) NULL COMMENT '來源流程' ,
  `SOURCE_DOC_ID` VARCHAR(25) NULL COMMENT '來源文件ID' ,
  `ATTRIBUTE1` CHAR(1) NULL COMMENT '彈性檢核欄位1' ,
  `ATTRIBUTE2` CHAR(1) NULL COMMENT '彈性檢核欄位2' ,
  `ATTRIBUTE3` CHAR(1) NULL COMMENT '彈性檢核欄位3' ,
  `ATTRIBUTE4` CHAR(1) NULL COMMENT '彈性檢核欄位4' ,
  `ATTRIBUTE5` CHAR(1) NULL COMMENT '彈性檢核欄位5' ,
  `CREATOR` VARCHAR(20) NOT NULL COMMENT '建立人' ,
  `CREATE_DT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP  COMMENT '建立日' ,
  `UPDATER` VARCHAR(20) NOT NULL COMMENT '更新人' ,
  `UPDATE_DT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP  COMMENT '更新日' ,
   PRIMARY KEY (`DOC_HEADER_ID`)
)
ENGINE = InnoDB
COMMENT = '傳票主檔';
CREATE TABLE `GL_JOURNAL_DETAIL` ( 
  `DOC_DETAIL_ID` BIGINT AUTO_INCREMENT NOT NULL COMMENT '文件明細ID' ,
  `DOC_HEADER_ID` BIGINT NOT NULL COMMENT '文件表頭ID' ,
  `JE_NO` VARCHAR(21) NOT NULL COMMENT '傳票ID' ,
  `LINE_NUM` CHAR(3) NOT NULL COMMENT '分錄行編號' ,
  `LEDGER_CODE` VARCHAR(2) NOT NULL COMMENT '帳本' ,
  `PERIOD_NUM` CHAR(2) NOT NULL COMMENT '會計期別' ,
  `ACCOUNT_TYPE` CHAR(1) NOT NULL COMMENT '科目類型' ,
  `ACCOUNT_CODE` VARCHAR(10) NOT NULL COMMENT '會計科目' ,
  `COMPANY_CODE` VARCHAR(4) NOT NULL COMMENT '公司代碼' ,
  `CURRENCY_CODE` VARCHAR(5) NOT NULL COMMENT '交易幣別' ,
  `BASE_CURRENCY` VARCHAR(5) NOT NULL COMMENT '本位幣' ,
  `DC_INDICATOR` CHAR(1) NOT NULL COMMENT '借貸方指示（C=貸、D=借）' ,
  `POSTING_KEY` CHAR(20) NOT NULL COMMENT '過帳碼' ,
  `DESCRIPTION` VARCHAR(200) NULL COMMENT '說明' ,
  `ENTER_AMT` DECIMAL(18,6) NULL COMMENT '交易金額' ,
  `ACCOUNT_AMT` DECIMAL(18,6) NULL COMMENT '本位幣金額' ,
  `INVOICE_NO` CHAR(10) NULL COMMENT '發票號碼' ,
  `IS_TAX` CHAR(1) NULL COMMENT '是否爲稅項' ,
  `TAX_CODE` VARCHAR(3) NULL COMMENT '稅項代碼' ,
  `TAX_AMT` DECIMAL(18,6) NULL COMMENT '本位幣稅金金額' ,
  `TAX_BASE_AMT` DECIMAL(18,6) NULL COMMENT '稅基金額' ,
  `PROFIT_CODE` VARCHAR(10) NULL COMMENT '利潤中心' ,
  `COST_CODE` VARCHAR(10) NULL COMMENT '成本中心' ,
  `PARTY_TYPE` VARCHAR(2) NULL COMMENT '交易對象類型' ,
  `TXN_KEY` VARCHAR(5) NULL COMMENT '交易碼' ,
  `CUSTOMER_CODE` VARCHAR(20) NULL COMMENT '客戶代號' ,
  `SO_NO` VARCHAR(20) NULL COMMENT '銷售訂單' ,
  `VENDOR_CODE` VARCHAR(20) NULL COMMENT '供應商代碼' ,
  `PO_NO` VARCHAR(20) NULL COMMENT '採購單' ,
  `ASSET_CODE` CHAR(12) NULL COMMENT '資產主編號' ,
  `ASSET_SUB_CODE` CHAR(4) NULL COMMENT '資產子編號' ,
  `MATERIAL_CODE` VARCHAR(40) NULL COMMENT '物料編號' ,
  `REF_DOC` VARCHAR(25) NULL COMMENT '參考文件' ,
  `PAYMENT_TERMS` VARCHAR(5) NULL COMMENT '付款條件' ,
  `PAYMENT_METHOD` VARCHAR(5) NULL COMMENT '付款方法' ,
  `BASELINE_DATE` DATETIME NULL COMMENT '基準日期' ,
  `PAYMENT_DUEDATE` DATETIME NULL COMMENT '付款到期日' ,
  `PAYMENT_BLOCK` CHAR(1) NULL COMMENT '付款凍結' ,
  `CLEARING_DATE` VARCHAR(255) NULL COMMENT '結清日期' ,
  `IS_CLEAR` CHAR(1) NULL COMMENT '是否爲結清項目' ,
  `CLEAR_COMPLETE` CHAR(1) NULL COMMENT '是否已結清' ,
  `CLEAR_JE_ID` VARCHAR(21) NULL COMMENT '結清傳票號碼' ,
  `CREATOR` VARCHAR(20) NOT NULL COMMENT '建立人' ,
  `CREATE_DT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP  COMMENT '建立日' ,
  `UPDATER` VARCHAR(20) NOT NULL COMMENT '更新人' ,
  `UPDATE_DT` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP  COMMENT '更新日' ,
   PRIMARY KEY (`DOC_DETAIL_ID`)
)
ENGINE = InnoDB
COMMENT = '傳票明細';

4-4 SA沒定, 自訂就好
4-5 ORM 自動填入
4-6 有新增公司代碼欄位, 根據實體如果有就填
4-7 可NULL 

