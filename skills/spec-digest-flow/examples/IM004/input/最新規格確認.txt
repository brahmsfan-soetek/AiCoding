1 D01 類型需要建立「一進一出」兩筆 MM_TRANSACTIONS -> 進出的欄位填寫未知，是否為 :「出」→ STOCK_TYPE_REDUSE = 'AVL'、PLANT_CODE/SLOC_CODE 填來源；「入」→ STOCK_TYPE_ADD = 'AVL'、PLANT_CODE/SLOC_CODE 填目標。並且其他欄位都相同。 => 正確！ 一進一出兩筆交易，STOCK_TYPE都是填寫'AVL', 而工廠/庫存地點各自填寫目標。其它欄位相同：是的！
2 批次控制邏輯 -> LOV 2.1 查詢有帶回 ENABLE_BATCH_CONTROL (Y/N)，但規格沒提到批次相關邏輯，目前 HTML 各類型皆可輸入批次且非必填，一樣採取 HTML的設計嗎 ? => 原本的設定思路是：由料號是否啟用批次控制，來決定批次欄位是否需要填寫！(和類型不相關，而是和料號本身相關。由料號主檔底表：IM_MATERIAL
.ENABLE_BATCH_CONTROL欄位來控制是否啟用批次。如果Y，表示啟用批認，則不論此料號進行怎樣的物料異動，都需要輸入數量的同時，輸入批次號(批次號目前無編碼規則，先挑選or手輸)(入庫時-手輸、出庫時-挑選)
3 前端庫存檢查 -> 「前端輸入出庫數量 > 畫面顯示的庫存水位時」，即時輸入警告檢查還是送出時由後端檢查 ? => 出庫數量>庫存水位：即時顯示警告，不送出後端檢查。
4 後端庫存不足時 -> 是否全部回滾拋出警告 ? => 後端庫存不足時，顯示出後端數量不足，交易不完成，全部回滾。
5 評價類別檢查時機 -> 選擇物料時當下檢查還是送出時檢查 => 選擇物料時當下檢查。
6 併發控制 -> 兩人同時操作到同一張暫存單據時，是否需要鎖 ? => 兩人同時操作到同一張暫存單據時，請讓後面的人，不能正常打開單據(顯示此筆單據已在編輯中，不允許同時操作)

p.s : 移除【成本影響】區塊, 僅留【成本計算摘要】
Deatil 表格主鍵改為 bigint 自增, 不再綁number