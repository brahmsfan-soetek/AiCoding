## 業務概述
<!-- 說明：用 2–4 句話簡述此程式的業務目的、主要使用者與核心操作（例如：此程式用於維護採購單，由倉管人員操作，支援新增、修改、審核與過帳）。 -->

### 不在此程式範圍
<!-- 說明：列出規格書明確排除、或容易被誤會要做但實際不在此程式範圍的功能。 -->

---

## 專案實作要求
<!-- 說明：列出此專案全域性的實作規範，凡跨頁面皆適用的技術約定統一寫在此處。 -->

- 開發時 Html 僅作為 UI 參考，實際行為如有與規格落差需確認不可直接實作
- `COMPANY_CODE`：從登入者 session 取得（已有現成實作）
- 後端查詢全都不做分頁和排序

### 前端元件採用
<!-- 說明：列出此專案統一使用的 UI 元件對照表，依實際專案替換元件名稱。 -->

<!-- 依專案替換 -->
| 類型 | 對應元件 |
|------|---------|
| table | TablesComponent |
| input | SInput2 |

### 後端實作
<!-- 說明：描述後端整體架構約定，例如 API 路由格式、回傳結構、錯誤處理方式、交易管理原則等。 -->

### 編碼規則
<!-- 說明：說明各類單據編號的產生規則，包含前綴、流水號位數、重置週期（如年重置）、由前端帶入或後端產生等。 -->

---

## 表格總覽
<!-- 說明：列出此程式所有會讀寫的資料庫表格，方便 PG 掌握資料邊界。 -->

### 主要表格
<!-- 說明：列出存放核心業務資料的表格（表頭、表身），並簡述其用途。格式建議：表格名稱 — 用途說明。 -->

### 查詢用表格
<!-- 說明：列出僅供查詢參照、不會由此程式寫入的表格（如 LOV 來源、代碼主檔等）。 -->

---

## 頁面規格
<!-- 說明：依頁面逐一描述 UI 元件、欄位行為與按鈕邏輯。若某頁面不存在，刪除對應小節即可。 -->

### 查詢頁面
<!-- 說明：描述查詢頁面的篩選條件、執行查詢的按鈕行為，以及查詢結果表格的欄位定義。 -->

#### 按鈕 (含業務邏輯)
<!-- 說明：列出查詢頁面上所有按鈕，描述每個按鈕的觸發動作、前端驗證、API 呼叫與頁面跳轉行為。 -->

#### 篩選條件
<!-- 說明：列出查詢頁面的篩選欄位，包含 UI 元件類型、預設值、必填與否，以及對應的後端查詢欄位。 -->

| UI 欄位名稱 | 必填 | UI 元件 | 對應欄位 | 預設值 | 說明 |
|------------|------|---------|---------|--------|------|
| <!-- 欄位名稱 --> | <!-- Y/N --> | <!-- 元件 --> | <!-- DB欄位 --> | <!-- 預設值 --> | <!-- 補充說明 --> |

#### 表格欄位 (查詢結果)
<!-- 說明：列出查詢結果表格的所有顯示欄位，標示是否可點擊、是否超連結，以及對應的資料來源欄位。 -->

| UI 欄位名稱 | UI 元件 | 對應欄位 | 說明 |
|------------|---------|---------|------|
| <!-- 欄位名稱 --> | <!-- 元件 --> | <!-- DB欄位 --> | <!-- 補充說明 --> |

---

### 新增頁面 / 修改頁面
<!-- 說明：描述新增與修改頁面共用的表單結構，若兩頁面差異較大可拆開描述。 -->

#### 按鈕 (含業務邏輯)
<!-- 說明：列出新增/修改頁面上所有按鈕，描述每個按鈕的觸發動作、前端驗證順序、API 呼叫與成功/失敗後的頁面行為。 -->

#### 表頭欄位
<!-- 說明：列出表單表頭區域的所有欄位，標示在新增模式與修改模式下是否可編輯，以及使用的 UI 元件與對應資料庫欄位。 -->

| UI 欄位名稱 | 新增可改 | 修改可改 | UI 元件 | 對應欄位 | 說明 |
|------------|---------|---------|---------|---------|------|
| <!-- 欄位名稱 --> | <!-- Y/N --> | <!-- Y/N --> | <!-- 元件 --> | <!-- DB欄位 --> | <!-- 補充說明 --> |

#### 表身欄位
<!-- 說明：列出表單表身（明細列）的所有欄位，標示可編輯性、條件顯示規則，以及對應的資料庫欄位。「條件顯示」填入使此欄位出現的條件（如特定異動類型），無條件限制則填「永遠顯示」。 -->

| UI 欄位名稱 | 新增可改 | 修改可改 | UI 元件 | 對應欄位 | 條件顯示 | 說明 |
|------------|---------|---------|---------|---------|---------|------|
| <!-- 欄位名稱 --> | <!-- Y/N --> | <!-- Y/N --> | <!-- 元件 --> | <!-- DB欄位 --> | <!-- 顯示條件或「永遠顯示」 --> | <!-- 補充說明 --> |

#### 連動性欄位
<!-- 說明：描述當某欄位變更時，會自動帶入或計算其他欄位的連動規則。格式建議：「當 [來源欄位] 變更時，自動帶入/計算 [目標欄位]，邏輯為 xxx」。 -->

#### 頁面底部摘要區塊
<!-- 說明：描述頁面底部的統計或彙總區塊（如成本合計、數量小計等），列出每個顯示欄位的計算來源與公式。若無此區塊，刪除本小節。 -->

| UI 欄位名稱 | 對應欄位 | 計算公式 / 說明 |
|------------|---------|---------------|
| <!-- 欄位名稱 --> | <!-- DB欄位 --> | <!-- 計算方式 --> |

---

### 特定功能頁面
<!-- 說明：針對不屬於標準新增/修改/查詢模式的頁面（如審核頁面、調整頁面、列印預覽等），複製本小節並修改標題以對應實際功能名稱。若無特定功能頁面，刪除本小節。 -->

#### 按鈕 (含業務邏輯)
<!-- 說明：同「新增頁面 / 修改頁面」的按鈕說明格式，針對此特定功能頁面描述。 -->

#### 表頭欄位
<!-- 說明：同上，針對此特定功能頁面的表頭欄位。 -->

| UI 欄位名稱 | 新增可改 | 修改可改 | UI 元件 | 對應欄位 | 說明 |
|------------|---------|---------|---------|---------|------|
| <!-- 欄位名稱 --> | <!-- Y/N --> | <!-- Y/N --> | <!-- 元件 --> | <!-- DB欄位 --> | <!-- 補充說明 --> |

#### 表身欄位
<!-- 說明：同上，針對此特定功能頁面的表身欄位，含條件顯示規則。 -->

| UI 欄位名稱 | 新增可改 | 修改可改 | UI 元件 | 對應欄位 | 條件顯示 | 說明 |
|------------|---------|---------|---------|---------|---------|------|
| <!-- 欄位名稱 --> | <!-- Y/N --> | <!-- Y/N --> | <!-- 元件 --> | <!-- DB欄位 --> | <!-- 顯示條件或「永遠顯示」 --> | <!-- 補充說明 --> |

#### 連動性欄位
<!-- 說明：同上，針對此特定功能頁面的連動規則。 -->

---

## LOV 資料來源
<!-- 說明：列出所有彈出式選單（LOV / Lookup）的資料來源定義，使用下方標準結構逐一描述。每個 LOV 獨立一個子節。 -->

### LOV 標準結構範本
<!-- 說明：複製以下結構，為每個 LOV 填寫對應資訊。 -->

**LOV 名稱**：<!-- 此 LOV 的功能名稱，例如「供應商選擇」 -->

| 屬性 | 內容 |
|------|------|
| 來源表 | <!-- 查詢的資料表名稱，例如 VENDOR_MASTER --> |
| 篩選條件 SQL | <!-- WHERE 條件，例如 WHERE COMPANY_CODE = :COMPANY_CODE AND ACTIVE_FLAG = 'Y' --> |
| 回傳欄位 | <!-- 選取後回傳的欄位清單，例如 VENDOR_CODE, VENDOR_NAME --> |
| 帶入目標 | <!-- 回傳值分別帶入的表單欄位，例如 VENDOR_CODE → 供應商代碼, VENDOR_NAME → 供應商名稱 --> |

---

## 驗證規則
<!-- 說明：列出所有欄位驗證規則，依觸發時機與頁面分類。 -->

### 必填檢查 (前端即時 + 送出時確認 + 後端檢查)
<!-- 說明：列出各頁面的必填欄位，前端在欄位離焦時即時提示，送出時再次全量驗證，後端 API 亦需 server-side 驗證。 -->

#### 查詢
<!-- 說明：查詢頁面中必須填寫才能執行查詢的欄位。 -->

#### 新增
<!-- 說明：新增頁面中必填的欄位，包含表頭與表身。 -->

#### 修改
<!-- 說明：修改頁面中必填的欄位，若與新增相同可標注「同新增」。 -->

### 特殊規則 - 即時發送後端 API 驗證
<!-- 說明：列出需要即時呼叫後端 API 做驗證的欄位（例如單號唯一性檢查、庫存量確認），描述觸發時機、呼叫的 API 與驗證邏輯。 -->

---

## 核心業務邏輯（過帳 / 審核 / 結算等）
<!-- 說明：描述此程式最核心的業務處理流程，例如過帳、審核、費用結算、庫存異動等。若有多個獨立核心流程，每個流程各建一個子節。若無特殊核心邏輯，刪除本節。 -->

### [流程名稱，例如：過帳邏輯]
<!-- 說明：填入實際流程名稱，例如「過帳邏輯」、「審核流程」、「費用結算」。 -->

#### 完整步驟
<!-- 說明：以有序清單描述此流程從觸發到完成的每個步驟，包含前置條件檢查、資料處理順序、資料庫寫入與異常處理。 -->

1. <!-- 步驟一 -->
2. <!-- 步驟二 -->
3. <!-- 步驟三（依實際步驟數增減）-->

#### 欄位對照表
<!-- 說明：列出此流程中來源資料與目標資料的欄位對應關係，例如從表身欄位寫入分錄表的欄位對照。 -->

| 來源欄位 | 目標欄位 | 轉換規則 / 說明 |
|---------|---------|---------------|
| <!-- 來源 --> | <!-- 目標 --> | <!-- 若有格式轉換或計算邏輯，於此說明 --> |

#### 科目 / 關鍵值取得邏輯
<!-- 說明：描述流程中特定關鍵值（如會計科目、成本中心、幣別匯率等）的查找規則，包含查哪張表、用哪些條件、找不到時的處理方式。若此流程不涉及科目，可改標題或刪除本小節。 -->

---

## 業務邏輯 / 特殊要求 / 刪除
<!-- 說明：列出不屬於上述任何章節、但需要特別實作的業務規則，例如：特殊計算邏輯、資料複製規則、狀態機轉換、刪除限制條件（何種狀態下不允許刪除）等。 -->

---

## 所有操作流程
<!-- 說明：以文字或步驟清單描述使用者完成每個主要任務的完整操作路徑，從進入頁面到最終儲存/送出，包含頁面間的跳轉順序。目的是讓 PG 能完整還原使用情境。 -->
