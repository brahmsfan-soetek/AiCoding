# {程式編號} {功能名稱} — 後端實作清單

> 初版日期：{YYYY-MM-DD}

---

<!-- 
  使用說明：
  - 以下區塊為通用骨架分類。若專案 CLAUDE.md 索引指向的後端規範文件有定義分類慣例，
    以該規範的分類取代本骨架。
  - 每個 task 必須包含：task id / 類型 / 描述 / 依賴（若有）/ 驗收條件。
  - **類型欄位**供 P3 SKILL 判斷測試策略，必須使用下表定義的 tag：

    | 類型 tag | 含義 | P3 backend 測試 / 對照策略 |
    |---|---|---|
    | `[validator]` | 驗證器 / 純函式 / 演算法 / 狀態機 | TDD（JUnit 純或 Mockito，完整 Red-Green） |
    | `[processor]` | API Processor（走 service → SQL → 回傳） | **無 mock-based 單元測試**；SG2 走 api_contract A## + current_schema 雙對照表（靜態檢查） |
    | `[sql]` | SQL YAML / DDL | 無 P3 測試；SG2 對照 current_schema 欄位 / 型別 |
    | `[entity]` | Entity / Domain 物件 | 無測試；SG2 對照 current_schema 欄位 / 型別 / nullable |
    | `[spi]` | SPI 介面 / 抽象類別 | 無測試（無邏輯） |

  - **Processor 類 task 必須列出「選填欄位清單」+ 對應 api_contract A## 小節**，供 P3 SG2 雙對照表審：
    例：`選填欄位：customerCode, customerName, riskLevel, collector`；`api_contract: A03` →
    P3 在 SG2 列 (1) `api_contract A03 Response 欄位` ↔ `預計實作 response shape`、(2) `current_schema 表欄位` ↔ `SQL/Entity 引用欄位` 兩張對照表給 PG 審；空值欄位行為由 PG 手測涵蓋（不再由 P3 mock test 擋）
  - 區塊可依實際需求增減，本模板僅規範必要欄位格式。
-->

## 一、資料層（Entity / Domain / SPI）

<!-- Entity 對應資料表；SPI 為抽象介面（如 SqlProvider）。命名須符合專案規範文件定義。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| B01 | `[entity]` | {Entity 類別名稱} | 對應 `{資料表名}`，含 {主要欄位清單} | — | {驗收條件} |
| B{xx} | `[spi]` | {SPI 名稱} | {描述} | — | {驗收條件} |

---

## 二、介面層（API Processor）

<!-- 依業務操作逐一列出。命名須符合專案規範文件定義。Processor 類必填「選填欄位」欄供 P3 SG2 覆蓋度檢核。 -->

| # | 類型 | 項目 | 說明 | 選填欄位 | 依賴 | 驗收條件 |
|---|------|------|------|---------|------|---------|
| B{xx} | `[processor]` | {API 名稱} | {功能描述：查詢/取得/新增/修改/刪除/業務動作/下拉} | {逗號分隔清單，例：customerCode, riskLevel} | {依賴} | {驗收條件} |

---

## 三、業務邏輯（Validator / 演算法 / 狀態機）

<!-- 純函式的驗證、計算、狀態推導。這類是 P3 backend 完整 TDD（JUnit/Mockito Red-Green）的主戰場；Processor 類不寫 mock test，走 SG2 雙對照表（api_contract + current_schema）。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| B{xx} | `[validator]` | {驗證器名稱} | {邏輯說明} | {依賴} | {驗收條件} |

---

## 四、業務邏輯（核心流程拆分）

<!-- 複雜業務操作（如過帳、審核）用子編號拆分。每個子步驟依實際類型標 tag。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| B{xx}-1 | `[validator]` / `[processor]` | {步驟名稱} | {邏輯說明} | {依賴} | {驗收條件} |
| B{xx}-2 | `[validator]` / `[processor]` | {步驟名稱} | {邏輯說明} | B{xx}-1 | {驗收條件} |

---

## 五、SQL 層

<!-- SQL YAML / DDL。類型固定為 [sql]，P3 backend 不為此類寫單元測試，SQL 行為由手測涵蓋。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| B{xx} | `[sql]` | {SQL 名稱} | {SELECT / INSERT / UPDATE / DELETE 摘要} | {依賴} | {驗收條件} |

---

## 六、整合點（外部服務 / SP / 第三方）

<!-- 需呼叫的外部服務、DB 預存程序或第三方 API。類型通常落在 [processor] 或 [spi]。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| B{xx} | `[processor]` / `[spi]` | {整合項目名稱} | {描述} | {依賴} | {驗收條件} |
