# {程式編號} {功能名稱} — 前端實作清單

> 初版日期：{YYYY-MM-DD}

---

<!-- 
  使用說明：
  - 以下區塊為通用骨架分類。若專案 CLAUDE.md 索引指向的前端規範文件有定義分類慣例，
    以該規範的分類取代本骨架。
  - 每個 task 必須包含：task id / 類型 / 描述 / 依賴（若有）/ 驗收條件。
  - **類型欄位**供 P3 SKILL 判斷測試策略，必須使用下表定義的 tag：

    | 類型 tag | 含義 | P3 frontend 測試 / 對照策略 |
    |---|---|---|
    | `[service]` | API 服務層（呼叫後端 API 的封裝） | 無 mock-based 測試；SG2 對照 api_contract A##（path / method / payload / response shape 靜態檢查） |
    | `[store-map]` | Store 內的 mapping helper（將後端欄位轉前端欄位） | 無 mock-based 測試；SG2 對照 api_contract A##（後端 shape → 前端 shape mapping 靜態檢查） |
    | `[store-action]` | Store action（loadXxx / submitXxx 等流程） | 無測試（ROI 低，由手測涵蓋） |
    | `[types]` | TypeScript interface / type 定義 | 無測試（typecheck 已蓋） |
    | `[page]` | 頁面元件（route 對應的 View） | 無測試（UI/UX 由 PG 手測） |
    | `[dialog]` | 彈窗 / Modal 元件 | 無測試（UI/UX 由 PG 手測） |
    | `[i18n]` | 語系檔（ar.json 等） | 無測試（靜態資料） |
    | `[router]` | 路由設定 | 無測試（靜態資料） |

  - 區塊可依實際需求增減，本模板僅規範必要欄位格式。
-->

## 一、資料層（型別定義與服務）

<!-- 每個程式通常至少有一個 types 檔與一個 service 檔。路徑須符合專案規範文件定義的位置。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| F01 | `[types]` | {types 檔名} | TypeScript 介面定義：{列出主要介面名稱} | — | {驗收條件} |
| F02 | `[service]` | {service 檔名} | API 服務層：{列出主要方法} | F01 | {驗收條件} |

---

## 二、Store 層

<!-- Store 分成兩類：mapping helper（契約轉換）與 action（流程）。應分開列。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| F{xx} | `[store-map]` | {helper 名稱，如 mapCustomerInfo} | 將後端欄位 `{後端欄位}` 轉為前端欄位 `{前端欄位}` | F01, F02 | {驗收條件} |
| F{xx} | `[store-action]` | {action 名稱，如 loadCustomerView} | {流程描述} | {依賴} | {驗收條件} |

---

## 三、介面層（頁面與元件）

<!-- 依專案規範文件定義的元件命名與結構慣例填入。涵蓋頁面入口、查詢頁、表單頁、Modal、Table 等。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| F{xx} | `[page]` 或 `[dialog]` | {元件/頁面名稱} | {功能描述} | {依賴的 task id} | {驗收條件} |

---

## 四、業務邏輯（連動與計算）

<!-- 前端連動邏輯。類型依實際實作位置決定（多數會落在 page/dialog 內）。 -->

| # | 類型 | 項目 | 觸發動作 | 連動結果 | 依賴 | 驗收條件 |
|---|------|------|---------|---------|------|---------|
| F{xx} | `[page]` | {欄位/元件名稱} | {用戶操作} | {系統回應} | {依賴} | {驗收條件} |

---

## 五、整合點（路由 / i18n / 外部整合）

<!-- 路由設定、i18n 語系檔等整合項目。路徑須符合專案規範文件定義的位置。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| F{xx} | `[router]` 或 `[i18n]` | {整合項目名稱} | {描述} | {依賴} | {驗收條件} |

---

## 六、驗證

<!-- 前端驗證規則：必填檢查、格式檢查、跨欄位一致性、狀態聯鎖等。多半落在 page/dialog 或 store 內。 -->

| # | 類型 | 項目 | 說明 | 依賴 | 驗收條件 |
|---|------|------|------|------|---------|
| F{xx} | `[page]` / `[dialog]` / `[store-action]` | {驗證規則名稱} | {觸發時機}：{驗證邏輯說明} | {依賴} | {驗收條件} |
