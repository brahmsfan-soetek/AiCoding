# Claude Code Insights

**2,586 messages across 146 sessions (171 total) | 2026-04-09 to 2026-05-20**

---

## At a Glance

- **What's working:** You're running a disciplined spec-driven pipeline—invoking structured workflows like `spec-p2-tasking` and `spec-p3-backend` to decompose modules into dozens of TDD-backed tasks with clean, scoped commits. You also handle messy iteration well, like reorganizing 20 commits into 7 readable ones, and you're not afraid to revert experiments that don't land. The habit of demanding direct DB verification over code inference has caught real bugs (collation mismatches, JSON path issues) that inference would have missed.

- **What's hindering you:** On Claude's side: it often starts solutioning before confirming scope, makes unauthorized inferences (adding features you didn't ask for, flipping semantics), and infers from adjacent code instead of consulting the authoritative source like the DB or spec. On your side: ambiguous or compound requests (especially around spec edits and SP behavior) leave room for Claude to fill gaps incorrectly—being explicit about what's out-of-scope and which source to verify against would prevent a lot of the back-and-forth.

- **Quick wins to try:** Try defining a Custom Skill for your repeated spec workflows so the scope-locking and source-of-truth rules are baked in. An MCP server for direct DB access would formalize the DB-first verification pattern you already enforce manually. And for SP-heavy modules, a pre-task hook that dumps current DDL into context could eliminate the 'Claude inferred from GL010 instead of querying' class of mistakes entirely.

- **Ambitious workflows:** As models improve, chain your `spec-p2-tasking` and `spec-p3-backend` workflows into a single overnight run that ingests a spec, tasks it, implements backend with TDD, scaffolds frontend, seeds data, and verifies end-to-end—your SO0062 result (30 tasks, 134 tests) shows this is within reach. For your iterative UI sessions, dispatch parallel agents to produce competing styling variants in separate worktrees with rendered screenshots, so you review finished options instead of nudging tweaks one at a time.

---

## Overview Stats

| Metric | Value |
|---|---|
| Messages | 2,586 |
| Lines | +98,306 / -11,911 |
| Files | 1,249 |
| Days | 23 |
| Msgs/Day | 112.4 |

---

## What You Work On

### AR002 Receipt Processing Module (~18 sessions)
Extensive work on the AR002 accounts receivable receipt module, including spec reconciliation with v1.6, P2/P3 frontend/backend workflows, attachment upload functionality, decimal/FX handling, validation logic, and stored procedure debugging. Claude was used heavily for TDD-driven implementation, iterative UI refinement of dialogs and forms, and integration testing with backend SPs.

### IM004 Inventory Migration Module (~7 sessions)
Development of the IM004 module covering `SP_IM_MIGO_CREATE` preview/posting, migration from Java to new GR-version stored procedures, real-time stock/price display, and cascading LOV fixes. Claude handled direct DB testing, schema collation debugging, frontend standards compliance, and SP documentation regeneration.

### SO0062/AR003 Sales Order Features (~5 sessions)
Full-stack implementation of the so0062 module via the `spec-p2-tasking` and `spec-p3-backend` workflows, producing task lists and 134+ passing unit tests across many commits. Claude also handled commit consolidation (20→7), validation bug fixes, navigation guards, and adapted explanation styles to user backgrounds.

### Homepage Dashboard & UI Polish (~6 sessions)
Iterative refinement of homepage panels including People/birthday cards, approval tracking, CEO quote carousel, seat map dialogs, and shortcut panels. Claude was used for rapid visual iteration, i18n integration across zh-TW/en-US, mock data generation, and consistent spacing/styling unification.

### Spec Documentation & Database Tooling (~8 sessions)
Generation of SP/View markdown documentation from live DDL dumps, spec clarification list refinement, SA assumption integration, and Quarkus dev environment setup (`.env`, Hibernate logs). Claude assisted with SQL imports via CLI, DB audits, and producing final spec statistics documents.

---

## What You Wanted

| Category | Count |
|---|---|
| Bug Fix | 34 |
| UI Styling Adjustments | 18 |
| Iterative Styling Adjustments | 15 |
| UI Styling Refinement | 15 |
| Code Explanation | 14 |
| UI Refinement | 13 |

## Top Tools Used

| Tool | Count |
|---|---|
| Bash | 3,772 |
| Edit | 3,274 |
| Read | 2,724 |
| Grep | 1,258 |
| TaskUpdate | 879 |
| Write | 570 |

## Languages

| Language | Count |
|---|---|
| Markdown | 1,246 |
| TypeScript | 857 |
| Java | 760 |
| JSON | 324 |
| YAML | 153 |
| JavaScript | 15 |

## Session Types

| Type | Count |
|---|---|
| Multi Task | 26 |
| Iterative Refinement | 17 |
| Single Task | 9 |
| Exploration | 6 |

---

## How You Use Claude Code

You work in **tight iterative loops**, especially on UI and styling tasks. Looking at your 18 ui_styling_adjustments and 15 iterative_styling_adjustments sessions, you rarely hand Claude a complete spec upfront—instead you make a change, look at it, and immediately request the next tweak. The homepage refinement sessions are emblematic: dept shortening, then birthday entries, then hover clipping, then carousel dots, then seat map dialogs, all in rapid succession. You're comfortable letting Claude run multi-step workflows (`spec-p2-tasking`, `spec-p3-backend` completing 30 tasks with 134 tests, 28 backend tasks with 117 tests), but you stay actively engaged and **interrupt the moment Claude drifts**.

Your interruptions are sharp and corrective rather than exploratory. When Claude looked at GL010 code to infer an SP signature, you cut in to demand DB-only verification. When Claude started generating test seed data instead of an SP syntax test script, you redirected immediately. When Claude over-modified a dialog width when you only wanted body padding removed, you required a revert. You also push back on **unauthorized inferences**—the AR002 clarification list session shows you pushing back multiple times when Claude added items like 'discount validation moved to frontend' that weren't yours to claim. You catch semantic errors others might miss (the inverted `IS_CLEAR` semantics, the wrong sign on the ANCV credit note, recomputing historical base-amounts with current rates).

You favor **clean git history and verified outcomes**—205 commits across 58 sessions, with explicit consolidation work (20 commits reorganized into 7) and recovery from Claude's git amend mistakes. You expect tests to pass and builds to verify before moving on. Your tooling pattern (3772 Bash, 3274 Edit, heavy Grep) shows you're operating in a real Java/TypeScript codebase where Claude must read, search, and validate against actual code rather than guess. The 299 'likely_satisfied' vs only 36 'dissatisfied' messages suggest Claude generally meets your bar, but the 35 'wrong_approach' frictions show you're not shy about course-correcting.

> **Key pattern:** Rapid iterative refinement with sharp, scope-tightening interrupts the moment Claude over-reaches or drifts off-target.

### User Response Time Distribution

| Bucket | Count |
|---|---|
| 2-10s | 98 |
| 10-30s | 324 |
| 30s-1m | 408 |
| 1-2m | 491 |
| 2-5m | 460 |
| 5-15m | 239 |
| >15m | 105 |

Median: **84.4s** · Average: **210.5s**

### Multi-Clauding (Parallel Sessions)

- Overlap Events: **36**
- Sessions Involved: **56**
- Of Messages: **11%**

You run multiple Claude Code sessions simultaneously. Multi-clauding is detected when sessions overlap in time, suggesting parallel workflows.

### User Messages by Time of Day (PT)

| Period | Count |
|---|---|
| Morning (6-12) | 665 |
| Afternoon (12-18) | 1,703 |
| Evening (18-24) | 218 |
| Night (0-6) | 0 |

### Tool Errors Encountered

| Type | Count |
|---|---|
| Command Failed | 209 |
| Other | 113 |
| Edit Failed | 31 |
| File Not Found | 18 |
| User Rejected | 13 |
| File Changed | 10 |

---

## Impressive Things You Did

Across 146 sessions spanning enterprise ERP modules (AR002, IM004, SO0062), you've driven 205 commits through disciplined spec-driven workflows combining backend TDD, frontend implementation, and database verification.

### Spec-Driven Multi-Phase Delivery
You consistently invoke structured workflows (`spec-p2-tasking`, `spec-p3-backend`) that decompose modules into dozens of typed tasks, then execute them with TDD discipline—one session produced 30 backend tasks with 134 passing tests across 16 clean commits. This phased approach (spec → tasking → backend → frontend → integration) keeps large features traceable and reviewable.

### Iterative UI Refinement With Clean Commits
You refine UI details (panel spacing, dropdown behavior, dialog layouts, icon experiments) through rapid back-and-forth, then consolidate the messy history into organized commits—at one point reorganizing 20 commits into 7 scoped ones. You're not afraid to revert experiments that don't land, keeping the final git history readable.

### Direct DB Verification Over Inference
You repeatedly redirect Claude away from inferring stored procedure signatures from related code and demand direct database queries instead, catching subtle bugs like collation mismatches and JSON path issues. This grounding in actual DB state—combined with regenerating SP documentation from live DDL dumps—keeps the spec, code, and database aligned.

### What Helped Most (Claude's Capabilities)

| Capability | Count |
|---|---|
| Multi-file Changes | 30 |
| Good Debugging | 11 |
| Correct Code Edits | 8 |
| Good Explanations | 5 |

### Outcomes

| Outcome | Count |
|---|---|
| Fully Achieved | 31 |
| Mostly Achieved | 20 |
| Partially Achieved | 3 |
| Not Achieved | 2 |
| Unclear | 2 |

---

## Where Things Go Wrong

Your sessions show strong productivity but recurring friction from Claude jumping to solutions before fully grasping scope, making unauthorized inferences, and occasionally exceeding output limits.

### Premature solutioning before clarifying scope
Claude frequently starts implementing or exploring before confirming what you actually want, forcing you to interrupt and redirect. Consider asking Claude to confirm intent on ambiguous requests, or explicitly state 'don't start until you've confirmed X' for non-trivial changes.

- Claude rendered business Selects as a static table when you wanted live demos, requiring full rework
- Claude started generating test seed data instead of an SP syntax test script, requiring interruption to redirect

### Unauthorized inferences and scope creep
Claude adds features, makes semantic changes, or expands scope beyond what you asked, requiring pushback and reverts. Try constraining requests with explicit 'only do X, do not change Y' framing, especially for spec/doc edits.

- Claude included an unrequested export feature in the AR002Q spec and inverted `IS_CLEAR` semantics during test data reset
- Claude over-modified dialog width when you only wanted body padding removed, requiring a revert

### Wrong source of truth for verification
Claude infers from adjacent code or assumptions instead of consulting authoritative sources (DB, specs, actual SP definitions), leading to incorrect implementations. When accuracy matters, explicitly tell Claude which source to verify against before coding.

- Claude inferred SP signature from GL010 code instead of querying the DB directly, prompting you to demand DB-only verification
- Claude used `standardPrice` from the valuation view when SP behavior required `avgPrice`, requiring correction

### Primary Friction Types

| Type | Count |
|---|---|
| Wrong Approach | 35 |
| Buggy Code | 24 |
| Misunderstood Request | 19 |
| Excessive Changes | 12 |
| User Rejected Action | 9 |
| Output Token Limit Exceeded | 2 |

### Inferred Satisfaction (model-estimated)

| Level | Count |
|---|---|
| Likely Satisfied | 299 |
| Dissatisfied | 36 |
| Satisfied | 25 |

---

## Existing CC Features to Try

### Suggested CLAUDE.md Additions

#### 1. Database Investigation
> When investigating stored procedures, schemas, or DB behavior, query the database directly via DDL/definitions rather than inferring from other Java/SP code. Do not extrapolate signatures from analogous modules.

*Why:* User explicitly interrupted Claude in multiple sessions (GL010 SP signature inference, SP test script generation) for inferring DB behavior from sibling code instead of querying the DB.

#### 2. Working Style
> Stay narrowly scoped to the user's request. Do not expand into adjacent refactors, add features (e.g., export) not asked for, or write broad implementation plans unless explicitly requested. Ask before broadening scope.

*Why:* Multiple sessions show friction from scope creep: unwanted AR002Q export feature, expanding into a public-form implementation plan, over-modifying dialog width, excessive exploration before IM004 refactor discussion.

#### 3. AR002 / Financial Conventions
> For AR002 / receipt / FX work: currency precision is driven by currency code (not a `decimals` prop). Historical base-amount values must NOT be recomputed with current FX rate. Credit note sign conventions: verify before seeding.

*Why:* User corrected these exact conventions across at least three separate AR002 sessions (decimals split rejected, base-amount recompute reverted, ANCV credit note sign fix).

#### 4. Git Workflow
> When working in a git worktree, always confirm file writes target the worktree path, not the main repo. When using `git amend`/`rebase`, confirm the target commit before modifying history.

*Why:* Claude wrote `ar002Types.ts` to the wrong repo path once, and a git amend mistakenly combined two commits requiring reset-and-redo.

#### 5. Frontend Conventions / i18n
> For i18n additions, add strings to the existing default `cv.json` rather than creating new prefixed namespaces unless specifically asked.

*Why:* Claude self-corrected an i18n prefix override in one session; codifying this avoids the recurring mistake.

### Feature: Custom Skills

Reusable markdown-defined slash commands for repetitive workflows.

**Why for you:** You already use `/effort`, `/plugin`, `spec-p2-tasking`, `spec-p3-backend` workflows. Formalizing your recurring AR002/IM004/SO0062 spec workflows (spec sync → tasks → backend TDD → frontend → seed data → commit) into explicit skills would standardize the 30+ multi-file change sessions you ran.

```bash
mkdir -p .claude/skills/spec-sync && cat > .claude/skills/spec-sync/SKILL.md <<'EOF'
# Spec Sync
1. Read latest spec under docs/specs/
2. Diff against existing task files
3. Update clarification list (scope to frontend/Java dev concerns only — exclude SP-internal issues)
4. Regenerate spec statistics
5. Do NOT add features not in spec (e.g., export)
EOF
```

### Feature: MCP Servers

Direct DB/tool access via Model Context Protocol.

**Why for you:** You repeatedly told Claude to query the DB directly instead of inferring SP signatures from sibling code. A MySQL MCP server would let Claude pull DDL/definitions natively and end this recurring friction. You also imported SQL via PowerShell with a near-miss '>' typo — an MCP would avoid shell-escaping risks.

```bash
claude mcp add mysql -- npx -y @modelcontextprotocol/server-mysql --host localhost --user dev --database your_db
```

### Feature: Hooks

Auto-run shell commands at lifecycle events.

**Why for you:** You run TypeScript/Java builds and tests after nearly every edit (117 tests, 134 tests sessions). A `PostToolUse` hook running `npm run build` or `mvn test -pl <module>` after `Edit`/`Write` on relevant paths would catch the Rollup-promoted warnings and entity-scan/JAR conflicts you hit before commits.

```json
// .claude/settings.json
{
  "hooks": {
    "PostToolUse": [{
      "matcher": "Edit|Write",
      "hooks": [{"type": "command", "command": "if echo \"$CLAUDE_FILE_PATH\" | grep -qE '\\.(ts|vue)$'; then npm run -s typecheck; fi"}]
    }]
  }
}
```

---

## New Ways to Use Claude Code

### Lock scope before exploring

Have Claude restate the deliverable in one line and list out-of-scope items before touching files.

Your top friction is `wrong_approach` (35) and `misunderstood_request` (19): unwanted export feature, expanded public-form plan, dialog-width over-edit, button-alignment confusion. Forcing a pre-flight scope confirmation on multi-step tasks would cut these. You already do this implicitly on slash workflows that succeed (`spec-p2/p3`); make it the default for ad-hoc requests too.

> Before making any changes, restate: (1) the exact deliverable in one sentence, (2) the files you expect to touch, (3) what is explicitly OUT of scope. Wait for my confirmation.

### DB-first for SP/schema questions

When a question involves stored procedures, schemas, or DB behavior, query the DB before reading any Java/TS code.

Sessions show Claude inferring SP signatures from GL010 instead of querying directly, using `standardPrice` vs `avgPrice` incorrectly, and a collation-mismatch bug that required deep DB investigation anyway. Defaulting to DB-first would shortcut multiple corrections and align with how you actually verified `SP_IM_MIGO_CREATE` end-to-end.

> For this SP/schema work: connect to the DB and dump the actual DDL/definition FIRST. Do not infer signatures or behavior from sibling Java/TS code. Show me what you found before proposing changes.

### Batch UI tweaks into single planned commit

When user signals 'iterative styling pass', plan all expected micro-tweaks up-front and commit once at the end.

You logged 18+18+15+15+13+9 = 88 sessions tagged as ui_styling/refinement, with many ending in commit consolidation (20 commits → 7). Asking Claude to keep a running edit list during a styling session and commit once at the end would avoid the amend/reset mistakes and reduce noise.

> We're entering an iterative UI polish session. Keep a running list of every change in a scratchpad. Do not commit until I say 'commit'. When I do, produce one clean conventional commit covering all changes.

### Prefer small responses to avoid token limit errors

Two sessions failed entirely due to exceeding the 500 output token max.

Set explicit response-length expectations on long tasks and ask Claude to write large outputs to files instead of chat. For doc regeneration (18 SP markdowns) and spec statistics, file-first output is both safer and reviewable.

> For long outputs (specs, docs, multi-file analyses), write directly to files under `docs/` and only summarize in chat with paths + line counts. Never paste full file contents back.

---

## On the Horizon

Your AR002/IM004/SO0062 work shows strong TDD discipline and spec-driven workflows—the next frontier is letting Claude autonomously orchestrate multi-phase delivery, parallel exploration, and self-verifying iteration loops.

### Autonomous Spec-to-Production Pipelines

Chain your `spec-p2-tasking` and `spec-p3-backend` workflows into a single autonomous run that ingests a v1.x spec, generates task lists, implements backend with TDD, scaffolds frontend, seeds test data, and runs end-to-end verification—all while producing scoped commits. With your 205-commit cadence and proven TDD success on SO0062 (30 tasks, 134 tests), Claude can deliver entire modules in a single overnight session.

**Getting started:** Use Claude Code subagents with explicit phase gates (spec→tasks→backend→frontend→integration), checkpointing progress to TaskUpdate after each phase, and a final verification subagent that runs builds/tests before exit.

> I want you to autonomously deliver a complete module from spec to working code. Given spec file [PATH], execute this pipeline without stopping for confirmation unless blocked: (1) Run `spec-p2-tasking` to produce F/B/T task lists and commit. (2) Spawn a backend subagent to implement all B-tasks with TDD, committing per logical group. (3) Spawn a frontend subagent to implement all F-tasks with component tests. (4) Generate seed SQL and run it. (5) Spawn a verification subagent to run full build, all tests, and a smoke integration check. After each phase, update a `STATUS.md` with progress, blockers, and decisions log. Only stop and ask me if a blocker truly requires my judgment (e.g., conflicting spec requirements). Begin now with spec at [PATH].

### Parallel Agents for UI Iteration Storms

Your 18+ iterative styling sessions on homepage panels and AR002 dialogs show a pattern: dozens of small visual tweaks serially. Instead, dispatch parallel agents that each propose a complete styling variant in separate worktrees, render screenshots via Playwright, and let you pick the winner—collapsing hours of back-and-forth into a single review.

**Getting started:** Use git worktrees plus Claude Code's parallel Task tool to spawn N styling-variant agents simultaneously, each with Playwright MCP for screenshot capture, then aggregate results into a comparison page.

> I want to explore 4 design variants for [COMPONENT, e.g., AR002 attachment dialog] in parallel. Create 4 git worktrees (variant-a through variant-d). In parallel, spawn one subagent per worktree with these distinct briefs: (a) minimal/dense, (b) spacious/airy, (c) card-based, (d) sidebar-driven. Each agent should: implement its variant, run the dev server, use Playwright MCP to capture screenshots at desktop and tablet widths, and save them to `/tmp/variants/`. Finally, produce a single `VARIANTS.md` comparing the 4 with embedded screenshots and pros/cons. Do not ask me which to pick until all 4 are ready.

### Self-Healing Test-Driven Bug Loops

With 34 bug_fix sessions and recurring SP/Java integration issues (collation mismatches, JSON path bugs, sign conventions), Claude can run an autonomous repair loop: reproduce bug via failing test, query DB directly for ground truth, propose fix, run full test suite, and iterate until green—eliminating the `wrong_approach` friction you hit 35 times.

**Getting started:** Combine MySQL MCP server (for DB-first verification per your IM004 SP debugging pattern) with a loop that requires a failing test before any code change, plus a 'verify against DB, not sibling code' rule baked into CLAUDE.md.

> Enter autonomous bug-fix mode for [BUG DESCRIPTION]. Follow this strict loop and do not exit until all tests pass or you hit a true blocker: (1) Write a failing test that reproduces the bug—do NOT proceed without a red test. (2) Before hypothesizing, query the actual database via MCP for any SP signatures, column types, collations, or data shapes involved. Never infer from sibling code. (3) Propose minimal fix with one-sentence root cause. (4) Apply fix, run the targeted test, then the full test suite. (5) If anything fails, return to step 2 with new evidence. (6) On green, run the build, then commit with a message naming the root cause. Log each iteration to `BUGFIX_LOG.md`. Begin now.

---

## Fun Ending

> **"Claude caught a dangerous '>' typo in a PowerShell SQL import command, preventing data loss"**
>
> While helping the user import a large SQL file into MySQL via CLI, Claude spotted that using '>' instead of '<' would have wiped the file instead of importing it—a small character with catastrophic consequences narrowly averted.
